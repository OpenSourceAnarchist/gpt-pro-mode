# Required: Your OpenRouter API Key
OPENROUTER_API_KEY="sk-or-..."

# Optional: Customize the models used.
# See https://openrouter.ai/models for a full list.
# Model for generating initial candidates (a fast, cheap model is recommended)
GENERATION_MODEL="deepseek/deepseek-chat-v3-0324:free"
# Model for synthesizing the final answer (a powerful, "smart" model is recommended)
SYNTHESIS_MODEL="deepseek/deepseek-r1-0528:free"

# Optional: App identity for OpenRouter leaderboards
# See: https://openrouter.ai/docs/features/app-attribution
YOUR_SITE_URL="http://localhost:8000"
YOUR_SITE_NAME="OpenRouter Pro Mode"

# Optional: Fine-tune the generation and tournament process
# Maximum number of tokens for each model generation.
MAX_OUTPUT_TOKENS=8192
# Number of generations required to trigger "Tournament Mode".
TOURNAMENT_THRESHOLD=20
# Number of candidates to synthesize in each group during Tournament Mode.
GROUP_SIZE=10
